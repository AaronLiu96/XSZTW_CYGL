<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sdm.cybk" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.NewStudentGroupActivity"/>
        <activity android:name=".ui.activity.SchoolBusActivity"/>
        <activity android:name=".ui.activity.RidingActivity"/>
        <activity android:name=".ui.activity.LifeActivity"/>
        <activity android:name=".ui.activity.GoSchoolActivity"/>
        <activity android:name=".ui.activity.NetDataActivity"/>
        <activity android:name=".ui.activity.SchoolGroupActivity"/>
        <activity android:name=".ui.activity.ComPlayActivity"/>
        <activity android:name=".ui.activity.DormitoryActivity"/>
    </application>

</manifest>
